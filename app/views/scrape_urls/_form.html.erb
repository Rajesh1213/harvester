<%= form_with(model: scrape_url) do |form| %>
  <% if scrape_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scrape_url.errors.count, "error") %> prohibited this scrape_url from being saved:</h2>

      <ul>
        <% scrape_url.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
